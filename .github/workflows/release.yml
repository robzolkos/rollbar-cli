name: Release

on:
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
            ext: .exe

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          VERSION: ${{ github.ref_name }}
          EXT: ${{ matrix.ext }}
        run: |
          mkdir -p dist
          go build -trimpath -ldflags "-s -w -X main.version=${VERSION}" -o "dist/rollbar-${GOOS}-${GOARCH}${EXT}" ./cmd/rollbar

      - name: Checksums
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          EXT: ${{ matrix.ext }}
        run: |
          cd dist
          sha256sum "rollbar-${GOOS}-${GOARCH}${EXT}" > "SHA256SUMS-${GOOS}-${GOARCH}.txt"

      - name: Upload assets to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/rollbar-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}
            dist/SHA256SUMS-${{ matrix.goos }}-${{ matrix.goarch }}.txt

  build-packages:
    runs-on: ubuntu-latest
    needs: build
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            goarch: amd64
            rpm_arch: x86_64
          - arch: arm64
            goarch: arm64
            rpm_arch: aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Install packaging tools
        run: sudo apt-get update && sudo apt-get install -y rpm

      - name: Download binary
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          curl -sL "https://github.com/robzolkos/rollbar-cli/releases/download/v${VERSION}/rollbar-linux-${{ matrix.goarch }}" -o rollbar
          chmod +x rollbar

      - name: Build .deb package
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          PKG_ROOT="rollbar-cli_${VERSION}_${{ matrix.arch }}"

          mkdir -p "${PKG_ROOT}/DEBIAN"
          mkdir -p "${PKG_ROOT}/usr/bin"
          mkdir -p "${PKG_ROOT}/usr/share/doc/rollbar-cli"

          cp rollbar "${PKG_ROOT}/usr/bin/rollbar"
          cp LICENSE "${PKG_ROOT}/usr/share/doc/rollbar-cli/copyright"

          cat > "${PKG_ROOT}/DEBIAN/control" << EOF
          Package: rollbar-cli
          Version: ${VERSION}
          Section: utils
          Priority: optional
          Architecture: ${{ matrix.arch }}
          Maintainer: Rob Zolkos <rob@zolkos.com>
          Description: CLI for reading and listing Rollbar items and occurrences
           A command-line interface for Rollbar focused on error triage.
          Homepage: https://github.com/robzolkos/rollbar-cli
          EOF

          sed -i 's/^          //' "${PKG_ROOT}/DEBIAN/control"

          dpkg-deb --build "${PKG_ROOT}"

      - name: Build .rpm package
        run: |
          VERSION=${GITHUB_REF_NAME#v}

          mkdir -p rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          mkdir -p rpmbuild/BUILDROOT/rollbar-cli-${VERSION}-1.${{ matrix.rpm_arch }}/usr/bin
          mkdir -p rpmbuild/BUILDROOT/rollbar-cli-${VERSION}-1.${{ matrix.rpm_arch }}/usr/share/licenses/rollbar-cli

          cp rollbar rpmbuild/BUILDROOT/rollbar-cli-${VERSION}-1.${{ matrix.rpm_arch }}/usr/bin/rollbar
          cp LICENSE rpmbuild/BUILDROOT/rollbar-cli-${VERSION}-1.${{ matrix.rpm_arch }}/usr/share/licenses/rollbar-cli/LICENSE

          cat > rpmbuild/SPECS/rollbar-cli.spec << EOF
          Name:           rollbar-cli
          Version:        ${VERSION}
          Release:        1
          Summary:        CLI for reading and listing Rollbar items and occurrences
          License:        MIT
          URL:            https://github.com/robzolkos/rollbar-cli

          %description
          A command-line interface for Rollbar focused on error triage.

          %files
          %license /usr/share/licenses/rollbar-cli/LICENSE
          /usr/bin/rollbar
          EOF

          rpmbuild --define "_topdir $(pwd)/rpmbuild" \
                   --define "_rpmdir $(pwd)/rpmbuild/RPMS" \
                   --target ${{ matrix.rpm_arch }} \
                   -bb rpmbuild/SPECS/rollbar-cli.spec

          cp rpmbuild/RPMS/${{ matrix.rpm_arch }}/*.rpm .

      - name: Upload packages to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            rollbar-cli_*_${{ matrix.arch }}.deb
            rollbar-cli-*.rpm

  aur-publish:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'release'
    steps:
      - name: Generate PKGBUILD
        run: |
          VERSION=${GITHUB_REF_NAME#v}

          # Download source tarball and calculate checksum
          curl -sL "https://github.com/robzolkos/rollbar-cli/archive/v${VERSION}.tar.gz" -o source.tar.gz
          SHA256=$(sha256sum source.tar.gz | cut -d' ' -f1)

          # Generate PKGBUILD
          cat > PKGBUILD << 'EOF'
          # Maintainer: Rob Zolkos <rob@zolkos.com>
          pkgname=rollbar-cli
          pkgver=VERSION_PLACEHOLDER
          pkgrel=1
          pkgdesc="CLI for reading and listing Rollbar items and occurrences"
          arch=('x86_64' 'aarch64')
          url="https://github.com/robzolkos/rollbar-cli"
          license=('MIT')
          depends=('glibc')
          makedepends=('go')
          source=("$pkgname-$pkgver.tar.gz::https://github.com/robzolkos/rollbar-cli/archive/v$pkgver.tar.gz")
          sha256sums=('SHA256_PLACEHOLDER')
          options=('!debug')

          build() {
              cd "$pkgname-$pkgver"
              export CGO_CPPFLAGS="${CPPFLAGS}"
              export CGO_CFLAGS="${CFLAGS}"
              export CGO_CXXFLAGS="${CXXFLAGS}"
              export CGO_LDFLAGS="${LDFLAGS}"
              export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw"
              go build -ldflags "-s -w -X main.version=${pkgver}" -o rollbar ./cmd/rollbar
          }

          package() {
              cd "$pkgname-$pkgver"
              install -Dm755 rollbar "$pkgdir/usr/bin/rollbar"
              install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
          }
          EOF

          # Remove leading whitespace from PKGBUILD
          sed -i 's/^          //' PKGBUILD

          # Replace placeholders
          sed -i "s/VERSION_PLACEHOLDER/$VERSION/" PKGBUILD
          sed -i "s/SHA256_PLACEHOLDER/$SHA256/" PKGBUILD

      - name: Publish to AUR
        uses: KSXGitHub/github-actions-deploy-aur@v3.0.1
        with:
          pkgname: rollbar-cli
          pkgbuild: ./PKGBUILD
          commit_username: ${{ secrets.AUR_USERNAME }}
          commit_email: ${{ secrets.AUR_EMAIL }}
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update to ${{ github.ref_name }}"

  homebrew-publish:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'release'
    steps:
      - name: Update Homebrew Formula
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          VERSION=${GITHUB_REF_NAME#v}

          # Download SHA256 files
          curl -sL "https://github.com/robzolkos/rollbar-cli/releases/download/v${VERSION}/SHA256SUMS-darwin-amd64.txt" -o sha256-amd64.txt
          curl -sL "https://github.com/robzolkos/rollbar-cli/releases/download/v${VERSION}/SHA256SUMS-darwin-arm64.txt" -o sha256-arm64.txt

          SHA256_AMD64=$(cut -d' ' -f1 sha256-amd64.txt)
          SHA256_ARM64=$(cut -d' ' -f1 sha256-arm64.txt)

          # Generate formula
          cat > rollbar-cli.rb << 'FORMULA'
          class RollbarCli < Formula
            desc "CLI for reading and listing Rollbar items and occurrences"
            homepage "https://github.com/robzolkos/rollbar-cli"
            version "VERSION_PLACEHOLDER"
            license "MIT"

            on_macos do
              if Hardware::CPU.arm?
                url "https://github.com/robzolkos/rollbar-cli/releases/download/v#{version}/rollbar-darwin-arm64"
                sha256 "SHA256_ARM64_PLACEHOLDER"
              else
                url "https://github.com/robzolkos/rollbar-cli/releases/download/v#{version}/rollbar-darwin-amd64"
                sha256 "SHA256_AMD64_PLACEHOLDER"
              end
            end

            def install
              binary_name = Hardware::CPU.arm? ? "rollbar-darwin-arm64" : "rollbar-darwin-amd64"
              bin.install binary_name => "rollbar"
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/rollbar --version")
            end
          end
          FORMULA

          # Remove leading whitespace and replace placeholders
          sed -i 's/^          //' rollbar-cli.rb
          sed -i "s/VERSION_PLACEHOLDER/${VERSION}/" rollbar-cli.rb
          sed -i "s/SHA256_ARM64_PLACEHOLDER/${SHA256_ARM64}/" rollbar-cli.rb
          sed -i "s/SHA256_AMD64_PLACEHOLDER/${SHA256_AMD64}/" rollbar-cli.rb

          # Clone tap repo, update formula, push
          git clone https://x-access-token:${GH_TOKEN}@github.com/robzolkos/homebrew-tap.git
          cd homebrew-tap
          mkdir -p Formula
          cp ../rollbar-cli.rb Formula/rollbar-cli.rb
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/rollbar-cli.rb
          git commit -m "Update rollbar-cli to ${VERSION}"
          git push
